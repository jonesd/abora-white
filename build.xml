<project name="abora-white" default="dist" basedir=".">
    <description>
    	Abora server written in Java and based on Udanax-Gold source.
    </description>

  <!-- set global properties for this build -->
  <property name="name" value="abora-white"/>
  <property name="Name" value="Abora-White"/>
  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="dist"  location="dist"/>
  <property name="lib.dir" location="lib"/>
    
  <target name="init">
    <tstamp/>
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source " >
    <javac srcdir="${src}" destdir="${build}" extdirs="${lib.dir}" />
  </target>

  <target name="dist" depends="compile"
        description="generate the distribution" >
    <mkdir dir="${dist}/lib"/>
    <jar jarfile="${dist}/lib/${name}-${DSTAMP}.jar" basedir="${build}"/>
  </target>

  <target name="clean"
        description="clean up" >
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>
  

  <target name="test" depends="compile">
  	<mkdir dir="docs/junit/report"/>
  	<junit printsummary="true">
  		 <formatter type="xml"/>
  		<test name="org.abora.white.tests.AllTests" todir="./docs/junit/report"/>
  		<classpath>
  			<pathelement path="${build}"/>
  		</classpath>
  	</junit>
  </target>
  
  <target name="test.report" depends="test">
  	<mkdir dir="docs/junit/html"/>
	  <junitreport todir="./docs/junit/report">
		  <fileset dir="./docs/junit/report">
		    <include name="TEST-*.xml"/>
		  </fileset>
		  <report format="frames" todir="./docs/junit/html"/>
	</junitreport>
  </target>

	<target name="javadoc" depends="compile">
	<mkdir dir="docs/api"/>
	<delete dir="docs/api"/>
		<javadoc destdir="docs/api"
           author="true"
           version="true"
           use="true"
           windowtitle="${Name} API">
           
   <packageset dir="${src}" defaultexcludes="yes">
      <include name="org/abora/white/**" />
      <exclude name="**/tests/**"/>
    </packageset>
       
    <doctitle><![CDATA[<h1>${Name}</h1>]]></doctitle>
    <bottom><![CDATA[<i>Copyright &#169; 2003 David G Jones. All Rights Reserved.</i>]]></bottom>
    <tag name="todo" scope="all" description="To do:" />
<!--    <group title="Group 1 Packages" packages="com.dummy.test.a*"/> -->
<!--    <group title="Group 2 Packages" packages="com.dummy.test.b*:com.dummy.test.c*"/> -->
<!--    <link offline="true" href="http://java.sun.com/products/jdk/1.2/docs/api/" packagelistLoc="C:\tmp"/> -->
<!--    <link href="http://developer.java.sun.com/developer/products/xml/docs/api/"/> -->
  </javadoc>
	</target>

</project>
